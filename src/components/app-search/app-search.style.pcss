.app-search {
  padding: 40px;
  text-align: center;
  margin-left: auto;
  margin-right: aito;
}
.app-search__title {
  font-weight: 300;
  font-size: 36px;
  margin-bottom: 40px;
}
.app-search__form {
}
.app-search__form-field {
}
.app-search__submit {
  cursor: pointer;
  color: var(--white);
  background-color: var(--green);
  border-color: var(--green--600);
  &:hover {
    opacity: 0.8;
  }
}
